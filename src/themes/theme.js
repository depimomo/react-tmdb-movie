import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
          main: '#E26D5A',
        },
        secondary: {
          main: '#EFCA08',
        },
      },
});

export default theme;
